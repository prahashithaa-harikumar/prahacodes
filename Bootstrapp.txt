const handleSubmit = async (e) => {
  e.preventDefault(); // Prevent form from submitting the traditional way

  // Assuming your data object is called dataText, which you want to send
  const dataText = {
    customerType: customerType,
    fullName: formData.fullName,
    bankAccountId: formData.bankAccountId,
    dob: formData.dob,
    address: formData.address,
    contactNumber: formData.contactNumber,
    aadharNumber: formData.aadharNumber,
    panCardNumber: formData.panCardNumber,
    email: formData.email,
    monthlyIncome: formData.monthlyIncome, // Fixed property name
    product: formData.product
  };

  try {
    const response = await fetch(
      'https://833d5a49-449-45e8-3396-799725cd4fcd.mock.pstmn.io/post',
      {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify(dataText)
      }
    );

    // Handle the response
    const result = await response.json();
    console.log(result);
  } catch (error) {
    console.error('Error:', error);
  }
};