@RestController
@RequestMapping("/api")
public class CustomerController {

    @Autowired
    private CustomerService customerService;

    @GetMapping("/process-file")
    public ResponseEntity<String> processFile(@RequestParam String filePath) {
        customerService.processFormDataFromFile(filePath);
        return ResponseEntity.ok("Data processed from file successfully");
    }
}