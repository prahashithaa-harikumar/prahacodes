import React from "react";
import { Box, Text, Heading } from "@chakra-ui/react";

const DataPreview = ({ formData }) => {
  return (
    <Box>
      <Heading as="h2" size="lg" mb={4}>
        Preview Data
      </Heading>
      <Text><strong>Customer Type:</strong> {formData.customerType}</Text>
      <Text><strong>Bank Account ID:</strong> {formData.bankAccountId}</Text>
      <Text><strong>Full Name:</strong> {formData.fullName}</Text>
      <Text><strong>Date of Birth:</strong> {formData.dob}</Text>
      <Text><strong>Address:</strong> {formData.address}</Text>
      <Text><strong>Contact Number:</strong> {formData.contactNumber}</Text>
      <Text><strong>Aadhar Number:</strong> {formData.aadharNumber}</Text>
      <Text><strong>PAN Card Number:</strong> {formData.panCardNumber}</Text>
      <Text><strong>Email:</strong> {formData.email}</Text>
      <Text><strong>Monthly Income:</strong> {formData.monthlyIncome}</Text>
      <Text><strong>Product:</strong> {formData.product}</Text>
      <Text><strong>ID Proof File:</strong> {formData.idProofFile?.name || "No file uploaded"}</Text>
      <Text><strong>Aadhar File:</strong> {formData.aadharFile?.name || "No file uploaded"}</Text>
      <Text><strong>PAN Card File:</strong> {formData.panCardFile?.name || "No file uploaded"}</Text>
    </Box>
  );
};

export default DataPreview;